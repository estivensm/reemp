<legend>Especialidades</legend>
<br>
<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripcion</th>
      <th>Categoria</th>
      <th>Codigo</th>
      <th colspan="2">Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @specialties.each do |specialty| %>
      <tr>
        <td><%= specialty.name %></td>
        <td><%= specialty.description %></td>
        <td><%= specialty.category_id %></td>
        <td><%= specialty.code %></td>
        <td>

          <%= link_to edit_specialty_path(specialty), remote: true, class: "btn btn-info" do %>
            <i class="fas fa-edit"></i>
          <% end -%>

          <%= link_to specialty, method: :delete, data: {
                confirm: '¿Estas Seguro?',
                'confirm-button-text': 'Borrar',
                'cancel-button-text': 'Cancelar',
                'confirm-button-color': '#CC0000',
                 text: 'Recuerda que el registro se borrara permanentemente',
                'image-url': ''}, class: "btn btn-danger" do %>
              <span class="glyphicon glyphicon-trash"></span> 
          <% end -%>
     
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

  <div class="col-md-12 paginate text-center title-table1">
      <%= will_paginate @specialties, renderer: BootstrapPagination::Rails, previous_label: "&laquo;", next_label: "&raquo;", inner_window: 1, outer_window: 0 %>
  </div>


<script type="text/javascript">

  <% if flash[:success].present?%>
        toastr["success"]("<%= flash[:success] %> <b> Exito!</b>");
  <% elsif  flash[:edit].present? %>
        toastr["info"]("<%= flash[:edit] %> <b> Exito!</b>");
  <% elsif  flash[:destroy].present? %>
        toastr["error"]("<%= flash[:destroy] %> <b> Exito!</b>");
  <% end -%>

</script>

